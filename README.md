#Java ExecutorService Sample
A sample usage of executor service to manage a thread pool in a simple way.

* Requires 1.6+

Just run ThreadExecutor. You can change the size of thread pool to see the behavior change.